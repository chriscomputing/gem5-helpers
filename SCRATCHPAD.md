# Scratchpad

Temporary notes for the current session.

## Current State

- The repository already includes two example gem5 output directories.
- The main script now acts as a thin wrapper around the reusable library.
- The library now includes dataframe-based analysis helpers for mean, extrema,
  and sorting by stat column.
- The dataframe remains wide, with one row per run and raw stat values.
